syntax = "proto3";

option go_package = "./protocol";

message Usecase {
	string name = 1;
	string policy = 2;
	string devices = 3;
	string endpoints = 5;
}

message Response {
	string result = 1;
}

service ConWire{
	rpc Push(Usecase) returns (Response) {}
}